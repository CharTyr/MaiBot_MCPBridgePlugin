# MCP桥接插件 - 配置文件示例
# 将 MCP (Model Context Protocol) 服务器的工具桥接到 MaiBot
# 
# 使用方法：复制此文件为 config.toml，然后根据需要修改配置
#
# ============================================================
# 📚 如何获取 MCP 服务器？
# ============================================================
#
# 【远程 MCP 服务（HTTP/SSE 方式）】
#   - 魔搭 ModelScope: https://mcp.modelscope.cn/
#     阿里云提供的 MCP 服务平台，有多种免费工具
#   - Smithery: https://smithery.ai/
#     MCP 服务器注册中心，可搜索各类 MCP 服务
#   - Glama: https://glama.ai/mcp/servers
#     MCP 服务器目录
#
# 【本地 MCP 服务（stdio 方式）】
#   需要安装 Node.js (npx) 或 uv (uvx)
#   - 官方服务器列表: https://github.com/modelcontextprotocol/servers
#   - 常用服务:
#     * npx @modelcontextprotocol/server-filesystem  (文件系统)
#     * npx @modelcontextprotocol/server-github      (GitHub API)
#     * uvx mcp-server-fetch                         (HTTP 请求)
#
# ============================================================

# ============================================================
# 插件基本信息
# ============================================================
[plugin]
name = "mcp_bridge_plugin"
version = "1.2.0"
config_version = "1.2.0"
enabled = true

# ============================================================
# 全局设置
# ============================================================
[settings]
# 🏷️ 工具前缀 - 用于区分 MCP 工具和原生工具
tool_prefix = "mcp"

# ⏱️ 连接超时（秒）
connect_timeout = 30.0

# ⏱️ 调用超时（秒）
call_timeout = 60.0

# 🔄 自动连接 - 启动时自动连接所有已启用的服务器
auto_connect = true

# 🔁 重试次数 - 连接失败时的重试次数
retry_attempts = 3

# ⏳ 重试间隔（秒）
retry_interval = 5.0

# 💓 心跳检测 - 定期检测服务器连接状态
heartbeat_enabled = true

# 💓 心跳间隔（秒）- 建议 30-120 秒
heartbeat_interval = 60.0

# 🔄 自动重连 - 检测到断开时自动尝试重连
auto_reconnect = true

# 🔄 最大重连次数 - 连续重连失败后暂停重连
max_reconnect_attempts = 3

# ============================================================
# 🆕 v1.2.0 高级功能（实验性）
# ============================================================
# 📦 启用 Resources - 允许读取 MCP 服务器提供的资源
# 启用后可使用 mcp_status(query_type="resources") 查看资源列表
# 使用 mcp_read_resource 工具读取资源内容
enable_resources = false

# 📝 启用 Prompts - 允许使用 MCP 服务器提供的提示模板
# 启用后可使用 mcp_status(query_type="prompts") 查看模板列表
# 使用 mcp_get_prompt 工具获取模板内容
enable_prompts = false

# ============================================================
# 🔌 MCP 服务器配置
# ============================================================
#
# ⚠️ 重要：JSON 格式说明
# ────────────────────────────────────────────────────────────
# 服务器列表使用 JSON 数组格式，需要用 三个单引号 ''' 包裹
# 这样 JSON 中的双引号就不需要转义，更易读
#
# 格式示例：
#   list = '''
#   [
#     { "name": "xxx", "enabled": true, ... }
#   ]
#   '''
#
# ❌ 错误写法（双引号需要转义，容易出错）：
#   list = "[{\"name\": \"xxx\"}]"
#
# ✅ 正确写法（三个单引号包裹，无需转义）：
#   list = '''
#   [{"name": "xxx"}]
#   '''
# ────────────────────────────────────────────────────────────
#
# 每个服务器的配置字段：
#   name      - 服务器名称（唯一标识，用于生成工具名）
#   enabled   - 是否启用 (true/false)
#   transport - 传输方式: "stdio" / "sse" / "http"
#   url       - 服务器地址（sse/http 模式必填）
#   command   - 启动命令（stdio 模式，如 "npx" 或 "uvx"）
#   args      - 命令参数数组（stdio 模式）
#   env       - 环境变量对象（stdio 模式，可选）
#
# ============================================================
# 配置示例（复制并修改）
# ============================================================
#
# HTTP 远程服务器示例：
# list = '''
# [
#   {
#     "name": "my-remote-server",
#     "enabled": true,
#     "transport": "http",
#     "url": "https://your-mcp-server.com/mcp"
#   }
# ]
# '''
#
# stdio 本地服务器示例：
# list = '''
# [
#   {
#     "name": "filesystem",
#     "enabled": true,
#     "transport": "stdio",
#     "command": "npx",
#     "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/dir"]
#   }
# ]
# '''
#
# ============================================================

[servers]
list = "[]"
