# MCP桥接插件 - 配置文件示例
# 将 MCP (Model Context Protocol) 服务器的工具桥接到 MaiBot
# 
# 使用方法：复制此文件为 config.toml，然后根据需要修改配置

# ============================================================
# 插件基本信息
# ============================================================
[plugin]
name = "mcp_bridge_plugin"
version = "1.0.0"
config_version = "1.0.0"
# 是否启用插件
enabled = true

# ============================================================
# 全局设置
# ============================================================
[settings]
# 🏷️ 工具前缀 - 用于区分 MCP 工具和原生工具
# 生成的工具名格式: {前缀}_{服务器名}_{工具名}
tool_prefix = "mcp"

# ⏱️ 连接超时 - 连接 MCP 服务器的超时时间（秒）
connect_timeout = 30.0

# ⏱️ 调用超时 - 工具调用的超时时间（秒）
call_timeout = 60.0

# 🔄 自动连接 - 启动时自动连接所有已启用的服务器
auto_connect = true

# 🔁 重试次数 - 连接失败时的重试次数
retry_attempts = 3

# ⏳ 重试间隔 - 重试之间的等待时间（秒）
retry_interval = 5.0

# ============================================================
# 🔌 MCP 服务器配置
# ============================================================
# 
# 配置方式：使用 JSON 数组格式
# - 可在 WebUI 中编辑
# - 也可直接编辑此文件（用单引号包裹 JSON，避免转义）
#
# 每个服务器配置字段说明:
# • name: 服务器名称（唯一标识）
# • enabled: 是否启用 (true/false)
# • transport: 传输方式 (stdio/sse/http)
# • url: 服务器地址 (sse/http 模式)
# • command: 启动命令 (stdio 模式，如 npx/uvx)
# • args: 命令参数数组 (stdio 模式)
# • env: 环境变量对象 (stdio 模式，可选)
#
# ============================================================
# 配置示例
# ============================================================
#
# 单行写法（用单引号包裹）:
# list = '[{"name": "howtocook","enabled": true,"transport": "http","url": "https://example.com/mcp"}]'
#
# 多行写法（用三个单引号包裹，更清晰）:
# list = '''
# [
#   {
#     "name": "howtocook",
#     "enabled": true,
#     "transport": "http",
#     "url": "https://mcp.api-inference.modelscope.net/c9b55951d4ed47/mcp"
#   },
#   {
#     "name": "filesystem",
#     "enabled": false,
#     "transport": "stdio",
#     "command": "npx",
#     "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/dir"]
#   }
# ]
# '''
#
# ============================================================

[servers]
# 默认为空，请根据需要添加 MCP 服务器
list = '[]'
